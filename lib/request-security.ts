import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
const buckets = new Map<string, { count: number; resetAt: number }>();
const LIMITS = { login: { limit: 10, windowMs: 15 * 60_000 }, upload: { limit: 30, windowMs: 60 * 60_000 }, share: { limit: 60, windowMs: 5 * 60_000 }, signed: { limit: 120, windowMs: 5 * 60_000 } } as const;
export function requestIp(req: Request | NextRequest) { return req.headers.get('x-forwarded-for')?.split(',')[0]?.trim() || req.headers.get('x-real-ip') || 'unknown'; }
export function rateLimit(req: Request | NextRequest, category: keyof typeof LIMITS) { const config = LIMITS[category]; const key = `${category}:${requestIp(req)}`; const now = Date.now(); const current = buckets.get(key); if (!current || current.resetAt <= now) { buckets.set(key, { count: 1, resetAt: now + config.windowMs }); return null; } current.count += 1; if (current.count > config.limit) return NextResponse.json({ error: 'Too many requests. Please try again later.' }, { status: 429 }); return null; }
export function requireSameOrigin(req: Request) { const site = process.env.PUBLIC_SITE_URL; if (!site) return null; const expected = new URL(site).origin; const origin = req.headers.get('origin'); if (origin) return origin === expected ? null : NextResponse.json({ error: 'Cross-origin request blocked.' }, { status: 403 }); const referer = req.headers.get('referer'); if (referer) { try { return new URL(referer).origin === expected ? null : NextResponse.json({ error: 'Cross-origin request blocked.' }, { status: 403 }); } catch { return NextResponse.json({ error: 'Invalid request origin.' }, { status: 403 }); } } if (req.headers.get('cookie')?.includes('nv_session=')) return NextResponse.json({ error: 'Origin or Referer required.' }, { status: 403 }); return null; }
