import Link from 'next/link'; import { redirect } from 'next/navigation'; import { requireUser } from '@/lib/auth'; import { db } from '@/lib/db';
export default async function Tags(){try{await requireUser()}catch{redirect('/login')}const tags=await db.tag.findMany({include:{_count:{select:{files:true}}},orderBy:{name:'asc'}});return <main className="shell" style={{paddingTop:32,paddingBottom:80}}><Link href="/admin" className="muted">← Dashboard</Link><p className="accent" style={{marginTop:60}}>TAXONOMY</p><h1>Tags</h1><div className="grid-auto" style={{marginTop:28}}>{tags.map(t=><div className="glass" style={{padding:22}} key={t.id}><h3>#{t.name}</h3><p className="muted">{t._count.files} files</p></div>)}</div>{!tags.length&&<p className="muted">No tags yet.</p>}</main>}
